# ModBridge Environment Configuration
# Copy this file to .env and adjust values as needed

# Log level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# Timezone for container
TZ=UTC

# Version tag for Docker image (when building locally)
VERSION=dev

# Web UI Port (internal port in container)
WEB_PORT=:8080

# Additional configuration
# GOMAXPROCS=4
# GOGC=100
