run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - gofmt
    - govet

linters-settings:
  gofmt:
    simplify: true

issues:
  exclude-rules:
    # Exclude all linters from tests
    - path: _test\.go
      linters:
        - govet
